import { UsersUpdatePasswordPipe } from './users-update-password.pipe';

describe('UsersUpdatePasswordPipe', () => {
  it('should be defined', () => {
    expect(new UsersUpdatePasswordPipe()).toBeDefined();
  });
});
