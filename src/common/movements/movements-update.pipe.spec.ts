import { MovementsUpdatePipe } from './movements-update.pipe';

describe('MovementsUpdatePipe', () => {
  it('should be defined', () => {
    expect(new MovementsUpdatePipe()).toBeDefined();
  });
});
